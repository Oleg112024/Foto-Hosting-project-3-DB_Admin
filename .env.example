# ============================================================================
# ФАЙЛ КОНФИГУРАЦИИ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ - ШАБЛОН
# ============================================================================
# 
# Этот файл содержит шаблон всех переменных окружения для проекта
# Image Hosting Project. Для использования:
# 
# 1. Скопируйте этот файл как .env:
#    cp .env.example .env
# 
# 2. Измените все значения на реальные для вашего окружения
# 
# 3. НИКОГДА не коммитьте файл .env в репозиторий!
#    (он уже добавлен в .gitignore)
# 
# ВАЖНО: Все пароли и секретные ключи ОБЯЗАТЕЛЬНО должны быть изменены
#        в продакшн окружении для обеспечения безопасности!
# 
# ============================================================================

# ============================================================================
# КОНФИГУРАЦИЯ АДМИНИСТРАТОРА ПРИЛОЖЕНИЯ
# ============================================================================

# Email главного администратора приложения
# Используется для первоначального входа в систему
# Рекомендация: используйте реальный email для восстановления доступа
ADMIN_EMAIL=admin@example.com

# Пароль главного администратора
# КРИТИЧЕСКИ ВАЖНО: измените на сложный пароль в продакшене!
# Требования: минимум 12 символов, буквы, цифры, спецсимволы
# Пример сильного пароля: MyStr0ng!P@ssw0rd2024
ADMIN_PASSWORD=change_this_secure_password_in_production

# Список дополнительных администраторов (разделенные запятыми)
# Эти пользователи получат права администратора при создании аккаунта
# Формат: email1@domain.com,email2@domain.com,email3@domain.com
ADMIN_EMAILS=admin@example.com,administrator@localhost

# Автоматическое создание администратора при запуске
# БЕЗОПАСНОСТЬ: установите в 'false' для продакшена!
# true - создает администратора автоматически при каждом запуске
# false - требует ручной регистрации администратора (рекомендуется)
CREATE_ADMIN_USER=false

# ============================================================================
# БЕЗОПАСНОСТЬ FLASK ПРИЛОЖЕНИЯ
# Секретный ключ для Flask (ОБЯЗАТЕЛЬНО замените на случайную строку в продакшене)
# ============================================================================

# Секретный ключ для Flask (шифрование сессий, CSRF токены, подписи)
# КРИТИЧЕСКИ ВАЖНО: ОБЯЗАТЕЛЬНО замените на случайную строку в продакшене!
# Генерация: python -c "import secrets; print(secrets.token_hex(32))"
# Длина: минимум 32 символа, рекомендуется 64
# НИКОГДА не используйте значение по умолчанию в продакшене!
SECRET_KEY=your-secret-key-here-change-in-production

# ============================================================================
# НАСТРОЙКИ БАЗЫ ДАННЫХ POSTGRESQL
# ============================================================================

# Хост базы данных
# localhost - для локальной разработки
# db - для Docker Compose (имя сервиса)
# IP адрес - для удаленного сервера
DB_HOST=localhost

# Имя базы данных
# Будет создана автоматически при первом запуске
DB_NAME=images_db

# Пользователь базы данных
# postgres - стандартный суперпользователь PostgreSQL
# Рекомендация: создайте отдельного пользователя для продакшена
DB_USER=postgres

# Пароль базы данных
# ВАЖНО: измените на сложный пароль в продакшене!
# Избегайте спецсимволов, которые могут конфликтовать с URL
DB_PASSWORD=postgres

# Порт базы данных
# 5432 - стандартный порт PostgreSQL
# Измените если используете нестандартный порт
DB_PORT=5432

# ============================================================================
# НАСТРОЙКИ pgAdmin (ВЕБ-ИНТЕРФЕЙС УПРАВЛЕНИЯ БД)
# ============================================================================

# Email для входа в pgAdmin веб-интерфейс
# Доступ: http://localhost:5050
# Используйте валидный email формат
PGADMIN_DEFAULT_EMAIL=admin@admin.com

# Пароль для входа в pgAdmin
# ВАЖНО: используйте сложный пароль!
# Этот пароль защищает доступ к управлению базой данных
PGADMIN_DEFAULT_PASSWORD=your_secure_pgadmin_password_here

# ============================================================================
# НАСТРОЙКИ ЗАГРУЗКИ ФАЙЛОВ
# ============================================================================

# Максимальный размер загружаемого файла в байтах
# 52428800 байт = 50 МБ
# Расчет: размер_в_МБ * 1024 * 1024
# Примеры: 10MB=10485760, 100MB=104857600, 500MB=524288000
MAX_CONTENT_LENGTH=52428800

# Папка для хранения загруженных изображений
# Относительный путь от корня проекта
# Папка будет создана автоматически при первой загрузке
# ВАЖНО: убедитесь, что у приложения есть права записи
UPLOAD_FOLDER=images