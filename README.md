# Фото Хостинг 3.0 ()

Современный и масштабируемый сервис для загрузки, хранения и управления изображениями с возможностью получения прямых ссылок на загруженные файлы. Проект построен на базе Flask и PostgreSQL (хранением метаданных), обеспечивая высокую производительность, безопасность и удобство использования.


## 🚀 Возможности

- **Управление изображениями**: Загрузка (JPG, PNG, GIF, WebP), просмотр с пагинацией и удаление через веб-интерфейс.
- **Пользовательская система**: Регистрация, аутентификация и личные кабинеты для управления своими файлами.
- **Безопасность**: Санитизация имен файлов, проверка типов, ограничение размеров (до 50МБ) и безопасное хранение паролей.
- **Организация**: Автоматическое распределение файлов по папкам пользователей в хранилище.
- **Срок хранения**: Возможность установки срока хранения для изображений с автоматическим удалением просроченных файлов.
- **Администрирование**: Панель статистики, мониторинг производительности и встроенный pgAdmin для управления базой данных.
- **Логирование**: Подробное отслеживание действий пользователей с ротацией и архивированием логов.
- **Контейнеризация**: Полная поддержка Docker и Docker Compose для быстрого развертывания в любой среде.

## 📂 Структура проекта

```text
.
├── app.py                      # Основное Flask-приложение (маршруты и бизнес-логика)
├── db.py                       # Модуль взаимодействия с PostgreSQL (CRUD операции)
├── admin_app.py                # Маршруты и логика административной панели
├── admin_db.py                 # Базовые запросы для административной статистики
├── monitoring.py               # Система мониторинга и метрики для Prometheus
├── logging_config.py           # Конфигурация расширенного логирования и ротации
├── flash_utils.py              # Утилиты для группировки и вывода уведомлений (flash)
├── celery_app.py               # Конфигурация Celery для асинхронных задач
├── backup_db.py                # Скрипт для создания и восстановления дампов БД
├── init_db.py                  # Скрипт первичной инициализации базы данных
├── init-pgadmin.sh             # Скрипт настройки pgAdmin при запуске
├── db_pool.py                  # Реализация пула соединений с базой данных
├── daily_log_maintenance.py    # Скрипт ежедневного обслуживания и архивации логов
├── Dockerfile                  # Инструкции для сборки Docker-образа приложения
├── docker-compose.yml          # Основная конфигурация многоконтейнерной среды
├── docker-compose.optimized.yml # Расширенная конфигурация для высокой нагрузки (Production-режим)
├── nginx.conf                  # Конфигурация Nginx (Reverse Proxy)
├── nginx.optimized.conf        # Оптимизированные настройки Nginx для высокой нагрузки (Production-режим)
├── pgadmin_servers.json        # Конфигурация серверов для pgAdmin
├── requirements.txt            # Список Python-зависимостей проекта
├── database_optimization.sql   # SQL-скрипт для оптимизации структуры БД
├── deploy_optimized.sh         # Скрипт развертывания высокой нагрузки (Linux/Bash)
├── deploy_optimized.ps1        # Скрипт развертывания высокой нагрузки (Windows/PowerShell)
├── .env.example                # Пример файла с переменными окружения (шаблон)
├── .env                        # Файл переменных окружения (автосоздание из .env.example)
├── .gitignore                  # Список игнорируемых файлов для Git
├── templates/                  # Директория с HTML-шаблонами (Jinja2)
│   ├── base.html               # Базовый шаблон (Layout)
│   ├── index.html              # Главная страница
│   ├── login.html              # Страница авторизации
│   ├── register.html           # Страница регистрации
│   ├── upload.html             # Страница загрузки изображений
│   ├── images_list.html        # Список всех загруженных изображений
│   ├── statistics.html         # Публичная статистика
│   └── admin_statistics.html   # Панель администратора
├── images/                     # Хранилище загруженных изображений (автосоздание при запуске)
├── logs/                       # Директория с логами приложения (автосоздание при запуске)
└── backups/                    # Директория резервных копий БД (автосоздание при запуске)
```

## 🛠 Принцип работы

1. **Процесс загрузки**: При загрузке файла Flask проверяет его на соответствие разрешенным форматам и размеру. Имя файла очищается от опасных символов, после чего он сохраняется в подпапку пользователя внутри `images/`.
2. **Учет данных**: Информация о файле (оригинальное имя, размер, тип, владелец, срок истечения) мгновенно записывается в базу данных PostgreSQL.
3. **Обслуживание запросов**: Nginx принимает внешние запросы. Запросы к API и динамическим страницам проксируются на Flask (Gunicorn), а запросы к изображениям отдаются напрямую из файловой системы, что снижает нагрузку на приложение.
4. **Безопасность**: Доступ к защищенным разделам (загрузка, список файлов, админка) ограничен декораторами авторизации. Пароли пользователей никогда не хранятся в открытом виде.
5. **Автоматизация**: Приложение поддерживает автоматическую ротацию логов и может быть расширено для фоновой обработки задач через Celery.

## 📦 Инструкция по запуску

### Предварительные требования
- Установленные Docker и Docker Compose.
- Настроенный файл `.env` (создайте его на основе `.env.example`).

### Пошаговое развертывание

1. **Клонируйте проект**:
   ```bash
   git clone <repository-url>
   cd File_fotos
   ```

2. **Подготовьте окружение**:
   Отредактируйте файл `.env`, установив надежные пароли:
   ```env
   DB_NAME=image_hosting_db
   DB_USER=postgres
   DB_PASSWORD=your_secure_password
   SECRET_KEY=your_secret_key
   ADMIN_EMAIL=admin@example.com
   ADMIN_PASSWORD=admin_password
   ```

3. **Запустите контейнеры**:
   ```bash
   docker-compose up --build -d
   ```

4. **Доступ к интерфейсам**:
   - **Веб-сайт**: [http://localhost:8080](http://localhost:8080) (через Nginx)
   - **Панель управления БД (pgAdmin)**: [http://localhost:5050](http://localhost:5050)
     - Логин/Пароль: данные из вашего `.env` файла.

## 💾 Обслуживание базы данных

Для создания или восстановления резервных копий используйте скрипт `backup_db.py`:

```bash
# Создание резервной копии
python backup_db.py

# Восстановление данных из файла
python backup_db.py restore <имя_файла>.sql
```
Все резервные копии автоматически сохраняются в директории `backups/`.

## 🛠 Технологический стек
- **Язык**: Python 3.11
- **Фреймворк**: Flask (WSGI: Gunicorn)
- **База данных**: PostgreSQL 17.5
- **Веб-сервер**: Nginx (Reverse Proxy)
- **Контейнеризация**: Docker & Docker Compose
- **Интерфейс**: Bootstrap 5, Jinja2
- **Мониторинг**: Prometheus metrics, Logging rotation

